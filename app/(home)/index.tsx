import Animated from "react-native-reanimated";

const HomeScreen = () => {
  return (
    <Animated.View className="">
      <Animated.Text className="">HomeScreen</Animated.Text>
    </Animated.View>
  );
};

export default HomeScreen;
